# 天氣資料 JSON 分析與 SQLite 資料庫設計

## 1. JSON 結構分析

來源檔案 `weather_data.json` 包含了一週的農業氣象預報。主要資料位於 `cwaopendata.resources.resource.data.agrWeatherForecasts` 路徑下。

關鍵結構如下：
- **weatherProfile**: 總體天氣概況描述（文字）。
- **weatherForecasts**: 包含各地區的詳細預報數據。
    - **location**: 地區列表（例如：北部地區、中部地區等）。
        - **locationName**: 地區名稱。
        - **weatherElements**: 包含天氣要素。
            - **Wx**: 天氣現象（包含 `daily` 列表，有 `weather` 描述與 `weatherid`）。
            - **MaxT**: 最高氣溫（包含 `daily` 列表，有 `temperature` 數值）。
            - **MinT**: 最低氣溫（包含 `daily` 列表，有 `temperature` 數值）。

## 2. 資料庫設計 (SQLite)

為了將這些資料有效地儲存於 SQLite，建議將資料正規化為兩個主要表格：`regions` (地區表) 與 `forecasts` (預報表)。

### 表格 1: regions (地區表)
用於儲存不重複的地區名稱，避免重複儲存字串。

| 欄位名稱 | 資料型態 | 說明 |
| :--- | :--- | :--- |
| id | INTEGER PRIMARY KEY AUTOINCREMENT | 地區 ID |
| name | TEXT NOT NULL UNIQUE | 地區名稱 (如：北部地區, 中部地區) |

### 表格 2: forecasts (預報表)
用於儲存每日的天氣預報數值。同一地區、同一日期的資料整併為一筆記錄。

| 欄位名稱 | 資料型態 | 說明 |
| :--- | :--- | :--- |
| id | INTEGER PRIMARY KEY AUTOINCREMENT | 預報 ID |
| region_id | INTEGER NOT NULL | 對應 `regions.id` (外鍵) |
| date | TEXT NOT NULL | 預報日期 (格式：YYYY-MM-DD) |
| weather_desc | TEXT | 天氣描述 (對應 Wx.weather) |
| weather_id | TEXT | 天氣代碼 (對應 Wx.weatherid) |
| max_temp | INTEGER | 最高溫 (對應 MaxT.temperature) |
| min_temp | INTEGER | 最低溫 (對應 MinT.temperature) |
| created_at | DATETIME DEFAULT CURRENT_TIMESTAMP | 資料建立時間 |

### 關聯性
- `regions.id` 一對多 `forecasts.region_id`

## 3. Python 實作邏輯建議 (ETL 流程)

1. **讀取 JSON**: 載入 `weather_data.json`。
2. **連結資料庫**: 建立 SQLite 連線 (如 `weather.db`)。
3. **建立表格**: 執行 `CREATE TABLE` 若表格不存在。
4. **解析與插入資料**:
    - 遍歷 `location` 列表。
    - 檢查 `locationName` 是否存在於 `regions`，若無則插入並取得 `region_id`。
    - 遍歷該 location 的 `Wx`, `MaxT`, `MinT` 的 `daily` 列表。
    - 由於 `Wx`, `MaxT`, `MinT` 是分開的列表，需透過 `dataDate` 作為 key 將它們合併 (例如使用 Dictionary 暫存：`{ "2025-12-07": { "Wx": ..., "MaxT": ..., "MinT": ... } }`)。
    - 將合併後的資料插入 `forecasts` 表格。
